{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Chrome (Vite)",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost:5173",
            "webRoot": "${workspaceFolder}"
        }
    ]
}